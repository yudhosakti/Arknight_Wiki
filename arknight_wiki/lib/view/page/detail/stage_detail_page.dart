import 'package:flutter/material.dart';

class StageDetailPPage extends StatelessWidget {
  const StageDetailPPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Ini Stage Detail"),
    );
  }
}