import 'package:flutter/material.dart';

class ThemeDetailPage extends StatelessWidget {
  const ThemeDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Theme Detail Page"),
    );
  }
}